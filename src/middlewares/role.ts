// Re-export for consistent import paths
export { requireRole as roleMiddleware, requireInstructor, requireAdmin, requireSuperAdmin, requireMinimumRole, requirePermission, requireOwnership } from './roleBasedAccess';
